# Warning: this name must stay in sync with the badges referenced in the README
name: 'tests MinGW'

on:
  push:
    branches:
      - 'master'
  pull_request:

jobs:
  mingw:

    runs-on: windows-latest

    defaults:
      run:
        shell: msys2 {0}

    steps:
      - uses: actions/checkout@v2

      - uses: msys2/setup-msys2@v2
        with:
          msystem: MINGW64 # Start a 64 bit Mingw environment
          update: true

      - name: Install dependencies
        run: |
          pacman -S --noconfirm --needed git make mingw-w64-x86_64-{toolchain,ffmpeg,python,python3-pillow,meson}

      - name: Build
        run: |
          ./configure.py
          make

      - name: Python import and dylib runtime test
        run: |
          . venv/bin/activate
          python -c 'import pynodegl, pynodegl_utils'
          ngl-python -h
